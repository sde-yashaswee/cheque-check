There are a few problems. Number one: when I log in, the user signs in—not signs up. Sign‑up is fine because onboarding then appears, but during sign‑in, for a fraction of a second the active business is not present, maybe it’s loading. The onboarding flow is activated automatically, which is wrong. You need to wait for the business to become active. This is a problem. Even if I have three or four businesses running, when I log back in I see the onboarding flow screen, which is incorrect. 
2. After we transitioned to the newer screen and added animation and optimization, we implemented a custom loading screen that remains active until the screen content is visible. However, this loading screen is not good because it shows the entire skeleton in a single line in the center of the screen. It should display a single sample screen—a generic screen that can be used anywhere when transitioning between different screens of the application. Please fix this; it looks very odd. 
3. https://cheque-check.vercel.app/cheques/create The problem is that on all the create routes, especially nested ones, the padding between the left and right edges of the screen is too large, causing the application to be centered incorrectly. Expand the layout to the left and right edges so it has a consistent style across the entire application. Ensure the left and right padding and margins are consistent on every page, not too large or too small, and that mobile layouts are properly accommodated. Please fix this and make the layout consistent throughout the application. 
4. Implement empty states and other types of data handling, such as empty, insufficient, and authorized data, in the reports page as well. It should be at the widget level, just as we have done on the dashboard. Apply the same approach to the reports page. Please copy and do it. I am referring to the data content wrapper. 