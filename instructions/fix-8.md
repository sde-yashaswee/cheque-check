1. Implement an OnBoarding Flow with Creating the Business > Selecting their Preferences like, Currency, Date and Time Format, Time Zone, Language Etc.
2. On the Auth Login SignUp, add the password eye, to show the password typed by the user on the password button, with the hint text as ....
3. Not Sure on the mobile phones, the B and M Characters are not properly being displayed, can yo ufix this please. Like what is the issue with the font here?
4. The Name of the Application is ChequeCheck and not CheckCheck, please change it anywhere where it occurs.
5. Create a Bank Object, Like this will be updated by the admin only, there is no policy to be changed by the User or Somebody else, So, when the User creWhen the user creates a bank account, they select the bank to associate with. The bank accounts table will include the bank ID, and the account is a separate object from the bank. The bank is not something the user deals with; it is only updated on my side. The user can read it and associate their account with the bank. There could be multiple banks in India, and I can add all of them here, allowing users to associate their account with a specific bank. 
6. Like we have the Bank Account Card on the specific page of the bank account, with the Contact Person, button :- Remove the contact person button from the bank account specific page card. Add it to the party-specific card. Then remove it from the bank account page. 

7. Can you Can you create the business switcher pop-up I'm talking about, not the strip. The strip should not be touched; the pop-up. Can you make it from the ChatCN or similar pop-up components? It looks odd, and the spacing doesn't feel right. Use those components and build on top of them. Do not make a custom implementation—just use those components and add your information. Do this for both the business switcher pop-up and the pop-up card. The global search requires using the component from the ChatC to implement. 

8. It should be very clear in the application that the terminology we are using is accounts, not banks. A bank is something associated with an account, and an account is the real object the user will handle. So everywhere, account should be used, not the bank terminology. Accounts is the right terminology. 

9. We haven't implemented a delete functionality anywhere, right? Basically, we need to be able to delete the business, the parties, the accounts, the checks, and also the current user profile completely. It should work with the cascade effect at that hierarchy level. If we delete the complete profile, everything related to the business parties, accounts, and checks should be removed. Once we remove the business, any checks associated with it and any parties associated with them should be removed. This works in this way. Implement a front-end delete button that triggers a pop-up requiring the user to type the name of the object being deleted—e.g., the business name—in a text box. Only after the correct name is entered should the delete button become active, allowing the deletion. 

Please do not touch the existing front-end or back-end or any existing logic. Implement only the above topics. Do not touch any other front-end, back-end, styling, or anything else. 